# Production environment — committed to source control (no secrets here).
# Vite reads this file automatically during `npm run build` (production mode).
# Cloud Build can override VITE_API_URL via the env: field on the build step.

# Backend Cloud Run service URL for project charles-schwab-poc-465918
VITE_API_URL=https://enterprise-ai-backend-611331021574.us-central1.run.app

VITE_APP_ENV=production
