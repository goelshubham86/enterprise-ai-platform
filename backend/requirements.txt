# ─── Web Framework ───────────────────────────────────────────
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
python-multipart==0.0.12

# ─── Google Cloud ─────────────────────────────────────────────
google-cloud-aiplatform==1.71.1
google-cloud-storage==2.18.2
google-cloud-secret-manager==2.21.1

# ─── AI / LangChain ──────────────────────────────────────────
langchain==0.3.7
langchain-google-vertexai==2.0.5
langchain-community==0.3.5
langchain-core==0.3.19
langchain-text-splitters==0.3.2
langgraph==0.2.45

# ─── Vector Store ─────────────────────────────────────────────
# ChromaDB: embedded persistent vector database for Phase 2.
# Replaced by Vertex AI Vector Search in Phase 5.
chromadb==0.5.23
langchain-chroma==0.1.4

# ─── PDF Parsing ──────────────────────────────────────────────
pypdf==5.1.0

# ─── Observability ────────────────────────────────────────────
opentelemetry-api==1.27.0
opentelemetry-sdk==1.27.0
opentelemetry-instrumentation-fastapi==0.48b0
# Pin OTLP exporter to match opentelemetry-sdk version.
# chromadb pulls in 1.11.1 as a transitive dep, which ships _pb2.py files
# generated with old protoc — incompatible with protobuf>=4.x (which
# google-cloud-aiplatform requires). Pinning here forces pip to upgrade it.
opentelemetry-exporter-otlp-proto-grpc==1.27.0

# ─── Dev / Testing ───────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2
